Nothing needs to be added to .gitignore since the changes only include HTML files (frontend/app-details.html and frontend/index.html), which are source files and not build artifacts, dependencies, or temporary files.